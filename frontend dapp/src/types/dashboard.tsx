// This file is not used by the Vite app at runtime.
// Kept as a placeholder to avoid Next.js-only imports and missing dependencies.
//
// If you need a real dashboard page/component, add it under `src/pages/*`.

import type { VotingStats } from './index';

export const mockChairpersonDashboardStats: VotingStats = {
  totalVoters: 9,
  totalProposals: 12,
  activeProposals: 3,
  closedProposals: 2,
  totalVotes: 5,
  delegateCount: 0,
};

